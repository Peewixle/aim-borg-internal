# openpyxl is needed for two things only: reading a UDBR dump, and generating
# the design workbook (BG-65). pandas comes with the checks.
#
# Everything else — the server, accounts, sessions, SQLite — is standard
# library, which is why there is no build step beyond this.
openpyxl>=3.1
pandas>=2.0
